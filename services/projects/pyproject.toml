[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["app"]
omit = ["app/main.py", "app/database.py", "app/config.py"]

[tool.coverage.report]
fail_under = 90
show_missing = true
